---
# ==============================================================================
# vm_create.yml — Create a new VM in Proxmox from ISO
# Called by AAP Job Template: "Proxmox - Create VM"
# ==============================================================================
- name: Create VM in Proxmox
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - ../group_vars/all.yml
  roles:
    - role: proxmox_vm_create
